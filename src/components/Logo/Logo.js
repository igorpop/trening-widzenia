import React from "react";
import Icon from '../../templates/icon/Icon';
import logoImg from '../../assets/images/LogoBig.svg'


const Logo = (props) => (
 <Icon src={logoImg} {...props} />
);

export default Logo;